{
  "name": "telerik-hackathon",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "ts-node ./server/app.ts",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd server/client && npm install && npm run build" ,
    "dev": "tsc && npm run build && npx nodemon --watch server -e ts --exec npx nodemon server/app"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "ts-node": "^9.1.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.11",
    "@types/node": "^15.0.2",
    "nodemon": "^2.0.7",
    "typescript": "^4.2.4"
  }
}